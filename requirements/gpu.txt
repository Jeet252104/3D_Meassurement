# GPU and Deep Learning (CUDA 12.1)
# Install PyTorch with CUDA support first
--index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchvision>=0.17.0

# Reset to default PyPI for remaining packages
--index-url https://pypi.org/simple

# 3D Reconstruction
# NOTE: pycolmap from PyPI does NOT have CUDA support!
# For GPU-accelerated COLMAP, install system package:
#   sudo apt install colmap
# Or use pycolmap-cuda (requires CUDA 12.x):
#   pip install pycolmap-cuda
pycolmap

# Depth Estimation
transformers>=4.35.0
accelerate>=0.25.0

# Marker Detection
opencv-contrib-python>=4.8.0

# API and Server
fastapi>=0.105.0
uvicorn[standard]>=0.25.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiofiles>=23.2.0

# GPU Monitoring (optional)
pynvml>=11.5.0
